<h1>My Tasks</h1>
<% @tasks.each do |task| %>
  <li><%= link_to task.title, "tasks/#{task.id}" %>
  <%= link_to 'Delete', task_path(task), method: :delete %></li>
<% end %>

<%= link_to "Add a new task", new_task_path %>
