<h1>Edit tasks</h1>
<%= form_with model: @task do |f| %>
  <%= f.label :title, 'Title' %>
  <%= f.text_field :title %>

  <%= f.label :details, 'Details' %>
  <%= f.text_field :details %>

  <p>Mark as completed
  <%= f.check_box :completed %></p>

  <%= f.submit 'Update'%>
<% end %>
